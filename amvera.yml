meta:
  environment: python
  toolchain:
    name: pip
    version: "3.10"
build:
  requirementsPath: requirements.txt
run:
  scriptName: telega.py
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
